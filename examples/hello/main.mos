#set document(
  title: "Hello, Mosaic",
  author: "Mosaic contributors",
  language: "en",
)

#set page(
  paper: "A4",
  margin: 24mm,
  numbering: "bottom-center",
)

#set text(
  font: "Noto Sans",
  size: 11pt,
  leading: 1.35,
)

= Hello, Mosaic

This document exercises the bundled Noto Sans family: every
glyph outline ships in the PDF as a subset of a real TrueType
font, addressed through a Type 0 CID-keyed font with a
/ToUnicode CMap. Copy/paste round-trips back to the source
UTF-8 across every script the font covers.

Latin coverage is wide: café culture, §1 legal citations, German
Straße addresses, ¶ pilcrows, © copyright marks, curly "smart"
quotes, Polish Łódź, Czech Příliš žluťoučký kůň, Hungarian
árvíztűrő, Romanian mâine. Math operators (≤ ≥ ≠ √ ∂ ∑ −) and
the fi/fl ligatures (real shaping, not a font hack) come through
too. The math glyphs aren't in Noto Sans Regular itself — they
resolve through automatic per-glyph fallback to the bundled Noto
Sans Math cut, mid-word, with no `#set text(font: ...)` ceremony.

Past Latin, the bundled cuts cover Cyrillic — Привет, Мозаика;
многоязычный набор — and Greek — Καλημέρα, κόσμε. *Italic
emphasis* and **bold** route through the real Italic and Bold
cuts of Noto Sans, not faux transforms; the same is true in
Cyrillic and Greek emphasis. Documents that need the Adobe Core
14 faces instead can pin them by name (`#set text(font:
"Helvetica")`) — that path stays available for byte-stable
golden PDFs and goes out through the existing WinAnsi +
/Differences encoder.

Lists work in two flavours. Unordered:

- bullets render as a hanging marker in the left gutter,
- *italic* and **bold** flow through unchanged inside an item,
- nested lists shift one indent step further right:
  - inner items still keep the hanging-indent contract,
  - mix and match emphasis here too if you like.

Ordered lists renumber from 1, regardless of the digits typed in source:

5. first step,
9. second step,
27. and a long third step that runs well past the right margin so the line breaker has to wrap it back onto a second line at the same indent as the first — proving the hanging indent survives across wraps.

#figure(
  image: "demo.png",
  caption: "A tiny bundled PNG, decoded and emitted as a flate-compressed DeviceRGB image XObject.",
)
