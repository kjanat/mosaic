id             = "mosaic"
name           = "Mosaic"
version        = "0.0.1"
schema_version = 1
authors        = ["Kaj Kowalski <info@kajkowalski.nl>"]
description    = "Mosaic (.mos) typesetting language support."
repository     = "https://github.com/kjanat/mosaic"

# NOTE: `repository` below is a developer-local placeholder. Zed clones it and
# expects `grammar.js` / `src/parser.c` at the repo root, but
# `tree-sitter-mosaic` currently lives in a subdirectory of the Mosaic
# monorepo. Until it is published as a standalone repo (e.g.
# github.com/kjanat/tree-sitter-mosaic), this extension is installable as a
# dev extension but the grammar will not fetch successfully. See README.md.
[grammars.mosaic]
repository = "file:///home/kjanat/projects/mosaic/crates/tree-sitter-mosaic"
rev        = "10f2f4e7800c40ae89bcd7472b982e2766dacc88"
